schemaVersion: 2.0

image: todo PG docker image
host: PG_HOST

ports:
  - name: PG_PORT
    default: 5432

commands:
  - export POSTGRES_PASSWORD=password
  - export POSTGRES_DB=dbuff
  - PGDATA=/run/sidecar/postgres PGPORT=$PG_PORT docker-entrypoint.sh postgres